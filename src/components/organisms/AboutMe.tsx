'use client';

import type { FC } from 'react';
import Section from '@/components/atoms/Section';

const AboutMe: FC = () => {
    return <Section id="about">About Me</Section>;
};

export default AboutMe;
